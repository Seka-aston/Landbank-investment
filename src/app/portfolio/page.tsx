import type { Metadata } from "next";
import { PortfolioOverviewScreen } from "./portfolio-screen";

export const metadata: Metadata = {
    title: "My Portfolio — Land Bank",
};

export default PortfolioOverviewScreen;
