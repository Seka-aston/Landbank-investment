import type { Metadata } from "next";
import { OpportunitiesScreen } from "./opportunities-screen";

export const metadata: Metadata = {
    title: "Investment Opportunities — Land Bank",
};

export default OpportunitiesScreen;
