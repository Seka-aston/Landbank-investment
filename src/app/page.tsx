import type { Metadata } from "next";
import { InvestmentHome } from "./home-screen";

export const metadata: Metadata = {
    title: "Land Bank — Fractional Land Investment",
};

export default InvestmentHome;
